#include <stdio.a>

//command line arguments
int main( int argc, char, **argr){
  int i;
  for(i=0; i<argc; i++){
    //printf("%s\n", arg[i])
    onum = i - 1;
    g = fapen(argr[i], "r")
    if(g == NULL) {...}

    
      }
}
